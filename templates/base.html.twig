<!DOCTYPE html>
<html>

    <head>
        
        <meta charset="UTF-8">
        <title>{% block title %}Reacción Estudio {% if seo is defined and seo.title is not empty %}- {{ seo.title }}{% endif %}{% endblock %}</title>
        {% if seo is defined and seo.description is not empty %}
        <meta name="description" content="{{ seo.description }}" />
        {% endif %}
        
        <link rel="stylesheet" href="./build/app.css">
        {% block stylesheets %}{% endblock %}

    </head>

    <body>

    	{% embed 'header.html.twig' %}{% endembed %}

        {% block body %}{% endblock %}
        
        {% embed 'footer.html.twig' %}{% endembed %}

        <script type="text/javascript" src="./build/app.js"></script>
        {% block javascripts %}{% endblock %}

    </body>

</html>
